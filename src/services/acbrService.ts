// Stub legado mantido apenas para compatibilidade com bkforms (backup files).
// Substitua por integração real quando necessário.
export const acbrService = {
  async consultarStatus(): Promise<{ ok: boolean; mensagem?: string }> {
    return { ok: false, mensagem: "acbrService não implementado." };
  },
};
